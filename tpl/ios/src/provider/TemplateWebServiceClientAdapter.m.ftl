<@header?interpret />
<#assign curr = entities[current_entity] />

#import "${curr.name}WebServiceClientAdapter.h"

/**
 * RPC class for {@link ${curr.name}} WebServiceClient adapters.
 */
@implementation CommentWebServiceClientAdapter

@end